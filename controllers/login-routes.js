//this file is for the login functions
const express = require('express');
const router = require("express").Router();

const loginPage = () => {
    //mock function
}





router.get('', (res, req) => {
    //not sure what to put in here 
})


module.exports = router;